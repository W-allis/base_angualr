import { Router } from '@angular/router/src/router';
// import { Route } from '@angular/compiler/src/core';

export class AsyncRouterService {
  constructor(
    // public router?: Router
    // public route: Route
  ) {
    // console.log(router)
  }
}